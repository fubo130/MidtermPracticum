1.Use "menu-file chooser" to load file.
2.Save(export) are also in the menu (export as .csv file).
3.Filter for each row you need to enter data on each specific text field and use the correspond filt button.
4.Sort can be done by click on the title for each column.
5.In order to edit a row, you need to select that row (or create a new row and edit it).
